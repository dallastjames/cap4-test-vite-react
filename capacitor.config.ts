import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dallastjames.cap4testvitereact',
  appName: 'cap4-test-vite-react',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
